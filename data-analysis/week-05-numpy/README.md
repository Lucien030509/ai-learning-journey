# Week 5: NumPy

Completed practice:

- `array_basics.py`: one-dimensional arrays, vectorized arithmetic, basic
  statistics, Boolean comparisons, and Boolean filtering.
- `array_statistics.py`: two-dimensional arrays, shape, indexing, and
  averages by row and column.

Key ideas: ndarray, shape, indexing, vectorized operations, mean, min, max,
axis=0, axis=1, and Boolean masks.

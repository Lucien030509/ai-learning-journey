# Week 6: Pandas

Completed practice:

- `student_table.py`: creates a DataFrame, selects a column, calculates an
  average, filters rows, and adds a Boolean column.
- `course_results.csv`: small course-results dataset with an intentional
  missing value.
- `read_csv.py`: reads CSV data with Pandas, counts missing values, calculates
  an average, and produces a numerical summary.

Key ideas: DataFrame, Series, column names, filtering rows, creating columns,
read_csv, NaN, isna, and describe.
